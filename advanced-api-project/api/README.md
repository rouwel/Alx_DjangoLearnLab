The ListView view is used to list all createsd book instances.
TheDetailView view is used to retrieve one specific book instance by its id.
CreateView view used to create a book instance. It provides a message that shows the user that the book instance has been created successfully.

The UpdateView view is used to update a book instance with new information.
The DeleteView view used to delete a book instance from the database.